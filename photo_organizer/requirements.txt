# Photo Organizer - Auto-tagging and organization
# Core dependencies
Pillow>=10.0.0
numpy>=1.24.0
tqdm>=4.65.0
click>=8.1.0

# Image analysis and auto-tagging
transformers>=4.35.0
torch>=2.0.0
torchvision>=0.15.0
open-clip-torch>=2.20.0
sentence-transformers>=2.2.0

# EXIF and metadata
piexif>=1.1.3
python-dateutil>=2.8.0

# OCR for text detection (album covers often have text)
easyocr>=1.7.0

# Image hashing for duplicate detection
imagehash>=4.3.0

# Database for caching tags
sqlalchemy>=2.0.0

# Configuration
pyyaml>=6.0.0
python-dotenv>=1.0.0
