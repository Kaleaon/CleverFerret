# Archived frontend workspace

The standalone `frontend/` workspace is currently inactive in this repository.

It was moved to `archive/frontend/` and dependency artifacts (such as lockfiles,
Yarn metadata, and `node_modules`) were removed from version control.

If frontend development resumes, reintroduce a clean `frontend/` workspace with
an explicit package manager choice and reproducible scripts.
